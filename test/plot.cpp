#include <iostream>
#include <vector>
#include <memory>

struct Vector {
    double x, y;
};

std::vector<Vector> point_cloud;

bool request_point_addition(Vector point) { // returns true if point is added
    // we need to figure out how many points are around the requested point
    
}

int main() {
    return 0;
}